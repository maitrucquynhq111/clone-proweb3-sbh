import { PageContainer } from '~app/components';
import { CommissionList } from '~app/features';

const Commission = () => {
  return (
    <PageContainer>
      <CommissionList />
    </PageContainer>
  );
};

export default Commission;
