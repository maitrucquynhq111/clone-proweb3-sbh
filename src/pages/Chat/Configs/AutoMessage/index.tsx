import { PageContainer } from '~app/components';
import AutoMessageFeature from '~app/features/chat-configs/AutoMessage';

const AutoMessage = () => {
  return (
    <PageContainer>
      <AutoMessageFeature />
    </PageContainer>
  );
};

export default AutoMessage;
