import { PageContainer } from '~app/components';
import LabelMessage from '~app/features/chat-configs/LabelMessage';

const Label = () => {
  return (
    <PageContainer>
      <LabelMessage />
    </PageContainer>
  );
};

export default Label;
