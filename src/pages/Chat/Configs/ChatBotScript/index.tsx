import { PageContainer, CommingSoon } from '~app/components';

const ChatBotScript = () => {
  return (
    <PageContainer>
      <CommingSoon />
    </PageContainer>
  );
};

export default ChatBotScript;
