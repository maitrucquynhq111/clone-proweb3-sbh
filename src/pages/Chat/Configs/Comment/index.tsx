import { PageContainer } from '~app/components';
import CommentConfig from '~app/features/chat-configs/CommentConfig';
// import { CommingSoon } from '~app/features/chat-configs/components';

const Comment = () => {
  return (
    <PageContainer>
      <CommentConfig />
    </PageContainer>
  );
};

export default Comment;
